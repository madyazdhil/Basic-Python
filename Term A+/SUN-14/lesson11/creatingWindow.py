#will be using a module = tkinter
import tkinter
#calling the module into the file

window = tkinter.Tk()
#saving the window into a variable

window.title("my First window")
#making a title

window.geometry("400x400")
#ccreating the size for the window

window.mainloop()


import random
#import the module into the file
#import needs to be ALWAYS AT THE TOP

#variable to store the result
penampungan = random.randrange(1, 10)
#               |          |    |   |
#     calling the module   |    |   |
#                   calling the function called randrange to randomize a number between 1 - 10


#calling the randomized values choosen by the module inside of the variable 'penampungan' and display it into the console
print(penampungan)